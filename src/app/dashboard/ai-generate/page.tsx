/**
 * AI 퀴즈 생성
 * 사용 훅: useGenerateQuizFromText, useGenerateQuizFromPdf, useGenerateExplanation
 */
export default function AiGeneratePage() {
  return (
    <main className="flex-1 p-4 md:p-8 max-w-3xl mx-auto">
      {/* TODO: AiGenerateForm, PdfUploadForm 컴포넌트 */}
      <p style={{ color: '#6a6a6a' }}>AI 퀴즈 생성 페이지</p>
    </main>
  );
}
