import JoinClient from '@/features/participants/ui/JoinClient';

export default function JoinPage() {
  return (
    <main className="stage flex-1 flex items-center justify-center min-h-screen p-4">
      <JoinClient />
    </main>
  );
}
